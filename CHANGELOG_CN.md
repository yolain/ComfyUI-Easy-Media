# 更新日志

---

## [1.3.0] - 未发布

### 新节点

- **多轨项目**： 目前适配了Minimax，主要用于简化流水线，它用来处理多轨循环片段的编码、采样与解码保存，支持指定片段开始位置与最大生成数量。
- **多轨项目视频合并**： 预览未合并的片段成品，支持自动与手动合并。

### 针对于 MiniMax H3 的优化

- **运动上下文与二采**：优化了一下原 `MotionContext` 与二采的搭配，当前在默认配置的采样方案中：`medium` 输出较为流畅，`light`由于做了sigmas分离所以未在一采中进行充分采样。
- **运动上下文潜空间显存**：片段之间及完成二采的项目文件中，高分辨率上下文只保留 CPU 上的尾部 22 帧音视频数据；低分辨率一采检查点仍完整保留，以支持稍后继续二采。
- **多轨编辑器（音频轨）**：增加了 `锁定音频` 功能，仅在多轨项目下适用，生成视频时音频将不再regenerate, 而是通过此音频来调度视频画面的产出。
- **多轨编辑器（音频轨）**：增加了 `复用音频` 即所有片段都可复用该音频（原先需要克隆到每个片段下），最大支持15秒不会被片段截断。
- **多轨编辑器（任务轨）**：多选任务片段时可统一调整 任务模式、衔接模式与参考图尺寸大小
- **多轨编辑器（任务轨）**：新增
- **多轨编辑器**：调整了诸多文案更便于理解
- **多轨任务输出**：调整 task_index 最小值为-1，当为-1时可以和之前的多轨编辑器输出一样，输出所有轨完整媒体信息。（受上一个优化的原因，不能直接在多轨编辑器输出完整内容）
- **多轨编辑器**：未引用 slot 资源时延迟加载文件及 URL 媒体，由多轨任务节点按需物化所需媒体，减少长视频任务不必要的媒体资源开销。
(现在只需连接 `TRACKS_INFO` 即可, 多轨编辑器输出的 `图像`、`视频`、`音频` 在无Slot资源模式下都将输出为None)

## [1.2.1] - 2026-08-22

### ✨ 新功能

- **多轨编辑器**：补充音频片段右键克隆音频，视频片段右键克隆视频

### 🐛 问题修复

- **多轨任务输出**：修复 `Minimax` 格式下某索引任务片段无音频/视频应输出为None


## [1.2.0] - 2026-08-21

### ✨ 新功能

- **清除H3MotionContext潜空间**: 适合使用H3 Motion Context时接在循环末尾添加该节点
- **对比视频**：补充 `并排对比` 模式、支持直接选择媒体对比和 `监听输出历史` 模式
- **媒体选择器**：补充支持多选图片项及全选当前路径下图片
- **多轨提示词增强器**：新增该节点，支持 h3-context-ir 等模型的提示词增强功能，支持本地llama模型使用
- **多轨编辑器**：补充用户提示词 A/B 输出功能，支持选择不同的用户提示词输出
- **多轨编辑器**：补充用户提示词 引用功能 支持 `<Picture 1>`、`<Audio 1>`、`<Video 1>` 、`@图片1` 等方式引用资源，支持多元素组合

### 🐛 问题修复

- **多轨编辑器**：两段任务相连修改前一段时间应增加总时长
- **媒体选择器**：修复媒体选择器中面包屑应支持递归子级
- **媒体选择器**：修复图片重选问题

## [1.1.4] - 2026-08-05

### ✨ 新功能

- **多轨编辑器**：新增 `MiniMax` 视频格式
- **MiniMax H3**：新增 `easy minimaxH3ToVideo`，支持文生视频、参考生视频和首尾帧生视频条件处理
- **媒体列表工具**：新增 `easy splitAudios` 和 `easy splitVideos`，将媒体列表拆分为独立输出
- **多轨编辑器**：影片轨道、音频轨道、字幕轨道支持拖拽左侧图标调整排序，每种类型的轨道数量调整最大限制为3个

### 🐛 问题修复

- **多轨编辑器**：修复`MiniMax` 格式下视频轨道和音频轨道没有片段时 `多轨编辑器` 和 `多轨任务输出` 不应该输出空片段。
- **多轨编辑器**：修复小于5秒时 `TRACK_INFO` 输出的 总时长应当为任务片段总实际时长，而非默认5秒。
- **多轨编辑器**：任务轨片段支持自由放置并保留空档，可在片段间直接添加新任务片段填补空隙。
- **多轨编辑器**：修复从片段边缘热区拖拽裁剪时，裁剪帧会随鼠标按下位置或画布缩放比例产生偏移的问题。
- **多轨编辑器**：修复画布缩放后时间尺点击位置计算错误，导致播放头定位及工具栏左右裁剪时间偏移的问题。
- **多轨任务输出**：`MiniMax` 格式按下一任务片段起点及媒体轨实际片段结尾裁剪音视频；最后一个任务从自身起点裁剪至媒体有效结尾，不输出尾部补黑或补静音。
- **多轨信息输出**：总时长按任务片段时长之和计算，自动跳过任务片段间的空档。
- **多轨编辑器**：修复添加轨道和删除轨道时，应该重新计算节点高度，而非自适应预览区高度。

## [1.1.3] - 2026-07-30

### ✨ 新功能

- **多轨编辑器**：新增任务标签打点，可将多个任务片段作为一轮循环任务
- **多轨编辑器**：支持将 SRT 文件导入字幕轨
- **多轨编辑器**：新增任务轨概览，可在轨道中展开查看各片段的提示词和参考图片
- **多轨音频输出**：新增裁剪音频输出模式，可输出裁剪后的音频片段和起始帧数，方便S2V使用
- **LTX流程简化**：新增`easy ltxMultiTrackEncode`及多个简化的LTX流程节点

### 🐛 问题修复

- **ASR转录**：优化分词断句问题
- **多轨任务输出**：提示词格式为 `default` 或 `promptRelay` 时，应当输出空系统提示词
- **多轨编辑器**：修复App模式下输入的音频列表只输出了第一个音频的问题
- **多轨编辑器**：修复音频轨与字幕轨无法在开头与片段之间额外添加片段的问题

## [1.1.2] - 2026-07-12

### ✨ 新功能

-**多轨到S2V输出**：输出裁剪的音频片段和起始帧数，方便S2V使用

### 🐛 问题修复

- **媒体选择器**：修复媒体选择器在上次选择了子目录后，重新打开媒体选择器时未停留在子目录的问题
- **LTXV参考视频**：优化 `LTXVMakeRefVideo` 的使用
- **多轨编辑器**：修复在多轨编辑器中，向左裁剪的裁剪方式与常规剪辑工具不统一
- **多轨编辑器**：修复音频片段裁剪后波形错误、双击音频重选失效

## [1.1.1] - 2026-07-11

### 🐛 问题修复

- **多轨编辑器**：补充了空状态下的提示信息，移除了图片项 720全景图功能，补充了单图预览
- **从路径合并视频**：优化视频处理，并添加音频可选项
- **对比视频**：修复默认情况下静音问题，并添加可保存视频的选项减少需多余添加视频保存节点的开销


## [1.1.0] — 2026-07-09

### ✨ 新功能

- **多轨编辑器**：添加初版多轨编辑器以及配套节点，支持多轨视频、音频编辑、片段编辑与预览
- **媒体选择器**：为媒体选择器建立目录的 store 缓存，解决频繁从后端获取列表数据的问题
- **分割多图像**：支持图片列表或图片批次类型的图片分割，适用于 `Bernini 多参考`等场景
- **从路径合并视频**：补充`截取帧数`以支持合并后裁剪时长

### 🐛 问题修复

- **多轨编辑器、时间轴编辑器**：修复节点创建时的默认的宽度与高度
- **媒体选择器**：修复资源排序应按`名称`、`创建时间`、`文件夹优先`
- **媒体选择器**：修复搜索后进入子目录时，关键词未被清空等问题

---

## [1.0.4] — 2026-06-16

### ✨ 新功能

- **保存视频**：增加 `hide&save` 选项，支持在保存视频的同时隐藏输出视频节点的输出
- **时间线编辑器（App 模式）**：为 `prompt_override` 添加 `[0-5s]` 时间范围的解析支持
- **时间线编辑器（UI 模式）**：子轨道添加`拖拽导入图片`的功能

### 🐛 问题修复

- **时间线编辑器（UI 模式）**：修复主轨中片段时长修改后子轨道如有图片应等比调整对应时长的问题
- **时间线编辑器（UI 模式）**：修复音频子目录导入后音频预览显示错误的问题

---

## [1.0.3] — 2026-06-06

### ✨ 新功能

- **Bernini 临时方案**：添加了`Bernini conditioning`和 `Bernini Model Patch` 节点，在未更新到 ComfyUI 支持 Bernini 时，提供了一个暂时方案
- **LTXV 参考视频**：针对于多参考 Lora [模型地址](https://huggingface.co/LiconStudio/LTX-2.3-Multiple-Subject-Reference)新添加的节点

### 🐛 问题修复

- **时间线编辑器（UI 模式）**：修复`节点高度`在`画布刷新`和`分辨率选项`切换时会被恢复成默认值的 bug
- **时间线编辑器（UI 模式）**：修复`整体编辑`提示词模式下，有些情况下不能编辑片段内容的 bug
- **时间线编辑器（UI 模式）**：修复节点和轨道高度自适应的问题，右键菜单新增`克隆片段`，以方便 `wan2.1 bernini` 和 `LTX2.3 R2V` 使用

---

## [1.0.2] — 2026-05-31

### 🐛 问题修复

- **时间线编辑器（App 模式）**：修复当 prompt_override 未严格按照提示词格式书写时，理应按片段均分默认时长的问题
- **时间线编辑器（App 模式）**：修复只用一段音频填补完整时间线，需过滤掉空音频再进行判断
- **时间线编辑器（UI 模式）**：修复当同个片段中包含不同格式时，输出资源与排序错误的问题

---

## [1.0.1] — 2026-05-27

### ✨ 新功能

- **工作流**：添加 wan2.2 循环片段示例工作流
- **前端优化**：添加选中片段时出现 `+` 号可以往前或往后增加片段，并修复了一些已知 bug

### 🐛 问题修复

- 修复从 output 和子目录导入的图片链接有错误，导致编辑器中图片和输出都被过滤的问题

---

## [1.0.0] — 2026-05-25

### 💥 破坏性变更

- `时长与帧率`修改的输入框只有在`失焦`后才会生效（即修改数字后需要 enter 键确认或点击输入框以外区域才能变更成功，减少出错概率）
- `时长输入框`步进变更，当格式为帧数时步进为`4`、秒数时步进为`1`
- 片段时长编辑不再影响其他片段，如修改后所有片段相加的时长超出总时长，则总时长将自动适配到所有片段的时长总和

### ✨ 新功能

- **时间线编辑器**：轨道自动适配高度，图像和音频片段需要双击才能进入媒体选择界面，避免误操作导致的频繁弹窗
- **时间线编辑器**：增加了动态参数注入设定，支持提示词模板格式+多媒体传入的方式使用时间轴编辑器
